###
# Preview
# ------------
# This workflow deploy & clean up the preview environment.
#
# IMPORTANT: Managed by LaunchPad DO NOT EDIT!
###

name: "Preview [LaunchPad]"

on:
  pull_request:
    types:
      - closed
    branches:
      - main
      - master
  workflow_run:
    workflows: [ "CI [LaunchPad]" ]
#    branches:
#      - main
#      - master
    types:
      - completed
      - requested

jobs:

