version: v1.0
name: launchpad-example
registry: gcr.io/bamboo-copilot-274418/launchpad-example
defaults:
  min: 0
  max: 5
environments:
  preview:
    min: 0
    max: 1
    environmentVariables: { }
  preProduction:
    environmentVariables: { }
  production:
    environmentVariables: { }
