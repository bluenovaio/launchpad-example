version: v1.0
name: launchpad-example
defaults:
  min: 0
  max: 5
environments:
  preview:
    min: 0
    max: 1
    environmentVariables: { }
  preProduction:
    environmentVariables: { }
  production:
    environmentVariables: { }
